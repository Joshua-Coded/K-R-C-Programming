#include <stdio.h>

int main()

{

  // this program counts character inputs; version 1

  /*
  long nc;
  nc = 0;

  while (getchar() != EOF)
    {
      nc++;
      printf("%ld\n", nc);
    }
  */
  double nc;
  for(nc = 0; getchar() != EOF; ++nc)
    {
      ;
       printf("%.0f\n", nc);
    }
}
